server:
  port: 9297

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kpavlunenko/bas-configs.git
          clone-on-start: true
          username: config_server
          password: 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

eureka:
  client:
    region: default
    registryFetchIntervalSeconds: 5
    service-url:
      defaultZone: http://discovery-service:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "health,refresh"
  health:
    readiness-state:
      enabled: true
    liveness-state:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true
